Programs seem to be functional

run "sudo python3 standard_trie_kernel_routing_XDP.py" 

- queries the kernel's routing table and redirects packets accordingly
- attaches programs to both interfaces f1 and f0 to properly redirect
